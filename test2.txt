#Author: Vineet Karkera
#!/usr/bin/tclsh

proc setListOfColumns {}{  
      set ::colNames [split $line ,]
      puts "Number of Columns in $::colNames are [llength $::colNames] " 
};

puts "Value of colNames outside of proc  - {$columns}-"
puts "Length of colNames outside is - [llength $::columns] -"

foreach x $::columns {	
	label [set f.labelHeader$x] -textvariable $x 
        pack [set f.labelHeader$x]
}

checkbutton .chk -text "Occupied" -variable occupied